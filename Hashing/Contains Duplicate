class Solution(object):
    def containsDuplicate(self, nums):

        # return True if len(nums)-len(set(nums)) else False
        hash_set = set() 
        for n in nums:
            if n in hash_set:
                return True
            hash_set.add(n)
        return False


